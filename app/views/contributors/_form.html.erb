<%= form_with(model: contributor, local: true) do |form| %>
  <% if contributor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contributor.errors.count, "error") %> prohibited this contributor from being saved:</h2>

      <ul>
      <% contributor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :amount_share %>
    <%= form.number_field :amount_share %>
  </div>

  <div class="field">
    <%= form.label :max_limit %>
    <%= form.number_field :max_limit %>
  </div>

  <div class="actions">
    <button type="submit" class="btn">Submit</button>
    <%= link_to 'Back', contributors_path, class: 'btn' %>
  </div>
<% end %>
